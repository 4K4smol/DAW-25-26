export class Tarea {
    constructor(tareaId = null, titulo = null, duracion = 0, completada = false) {
        tareaId = this.tareaId,
        titulo = this.titulo,
        duracion = this.duracion,
        completada = this.completada
    }
}